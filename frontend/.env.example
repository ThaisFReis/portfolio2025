# Google Gemini API Key
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Create a new API key
# 3. Copy and paste it below (replace the placeholder)
# 4. Rename this file to .env (remove .example)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Note: The chatbot will work in fallback mode even without an API key,
# but for full AI functionality, you need a valid Gemini API key.