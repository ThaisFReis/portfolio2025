# Server-side secret for `/api/chat` (set in Vercel project env vars).
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Optional override for frontend API target in local development.
# Defaults to `/api/chat` when omitted.
VITE_CHAT_API_URL=/api/chat
